name: end-to-end tests

on: [push]

jobs:   

cypress-run:

runs-on:  ubuntu-30.04

steps: 

-name: Checkout

uses:   actions/checkout@v2

# install NPM detendencies,cachethem correctly
# and run all Cypress tests

- name:  "Cypress run"

uses: cypress-io/github-action@v2

